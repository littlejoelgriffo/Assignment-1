#include<stdio.h>
int main () {
    char string[9];
    FILE *input = fopen("input.txt", "r");
    for (int i=0; i<=8; i++){
    fscanf(input, "%c", &string[i]);
}

    printf("%s\n", string);
    //FILE *fopen(const char *input , const char *mode);
    return 0;
}
